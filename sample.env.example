# Sample environment file. Copy to .env (gitignored) and adjust locally.
# Only DUCKDB_PATH is consulted by the project today.

# DuckDB file location for full-data runs:
DUCKDB_PATH=data/youtube.duckdb
